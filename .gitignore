*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build  # MUST be committed for Vite assets to deploy
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Legacy Python files (keep for reference but not needed for production)
/legacy-python/
/_archive/
/_cleanup/

# Scraper debug files
scrapers/debug_*.html

# Scraped image folders (legacy data, not needed in repo)
/scraped_images/
/scraped_images_webp/
/scripts/scraped_images/
/scripts/scraped_images_webp/
scripts/scraped_images (copy)/

# Google Console reports (analytics data, not code)
*.xlsx
prixretro.com-*.csv

# Documentation and personal notes (keep local only, not in repo)
*.md
!README.md

# Database backups
/database/*.sqlite
/database/*.sqlite-*

# IDE and OS files
.phpstorm.meta.php
_ide_helper.php
_ide_helper_models.php
production-data*.sql
public/set-password.php
